{% trans_default_domain 'ir_product' %}

<div class="page-header">
    <h1>{{ 'admin.product.new.title'|trans }}</h1>
</div>

<form action="{{ path('ir_product_admin_product_new') }}" {{ form_enctype(form) }} method="POST" class="form-horizontal" novalidate>
    {% include 'IRProductBundle:Admin/Product:form.html.twig' %}
    <div>
        <button type="submit" class="btn btn-primary">{{ 'admin.product.new.save'|trans }}</button>
        <a href="{{ path('ir_product_admin_product_list') }}" class="btn">{{ 'admin.product.new.cancel'|trans }}</a>        
    </div>         
</form>