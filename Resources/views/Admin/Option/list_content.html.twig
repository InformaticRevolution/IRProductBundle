{% trans_default_domain 'ir_product' %}

<div class="page-header">
    <h1>{{ 'admin.option.list.title'|trans }}</h1>
</div>

<div class="well well-sm">
    <a href="{{ path('ir_product_admin_option_new') }}" class="btn btn-primary">
        <i class="glyphicon glyphicon-plus"></i> {{ 'admin.option.action.new'|trans }}
    </a>
</div>  

{% if options|length %}
<table class="table table-striped">      
    <thead>
        <tr>
            <th>{{ 'admin.option.name'|trans }}</th>
            <th>{{ 'admin.option.public_name'|trans }}</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    {% for option in options %}
        <tr>
            <td>{{ option.name }}</td>
            <td>{{ option.publicName }}</td>
            <td>
                <div class="pull-right">
                    <a href="{{ path('ir_product_admin_option_edit', {'id': option.id}) }}" class="btn btn-primary">
                        <i class="glyphicon glyphicon-edit"></i>
                    </a>
                    <a href="{{ path('ir_product_admin_option_delete', {'id': option.id}) }}" class="btn btn-danger" >
                        <i class="glyphicon glyphicon-trash"></i>
                    </a>
                </div>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% else %}
<div class="well">
    {{ 'admin.option.list.no_results'|trans }}
</div>
{% endif %}