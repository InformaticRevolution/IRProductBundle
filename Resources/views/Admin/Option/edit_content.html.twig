{% trans_default_domain 'ir_product' %}

<div class="page-header">
    <h1>{{ 'admin.option.edit.title'|trans }}</h1>
</div>

<form action="{{ path('ir_product_admin_option_edit', {'id': option.id}) }}" {{ form_enctype(form) }} method="POST" class="form-horizontal" novalidate>
    {% include 'IRProductBundle:Admin/Option:form.html.twig' %}
    <div>
        <button type="submit" class="btn btn-primary">{{ 'admin.option.edit.save'|trans }}</button>
        <a href="{{ path('ir_product_admin_option_list') }}" class="btn">{{ 'admin.option.edit.cancel'|trans }}</a>         
    </div>         
</form>