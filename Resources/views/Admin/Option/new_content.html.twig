{% trans_default_domain 'ir_product' %}

<div class="page-header">
    <h1>{{ 'admin.option.new.title'|trans }}</h1>
</div>

<form action="{{ path('ir_product_admin_option_new') }}" {{ form_enctype(form) }} method="POST" class="form-horizontal" novalidate>
    {% include 'IRProductBundle:Admin/Option:form.html.twig' %}
    <div>
        <button type="submit" class="btn btn-primary">{{ 'admin.option.new.save'|trans }}</button>
        <a href="{{ path('ir_product_admin_option_list') }}" class="btn">{{ 'admin.option.new.cancel'|trans }}</a>        
    </div> 
</form>