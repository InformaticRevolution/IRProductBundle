{% trans_default_domain 'ir_product' %}

<div class="page-header">
    <h1>{{ 'admin.variant.edit.title'|trans }}</h1>
</div>

<form action="{{ path('ir_product_admin_variant_edit', {'id': variant.id}) }}" {{ form_enctype(form) }} method="POST" class="form-horizontal" novalidate>
    {% include 'IRProductBundle:Admin/Variant:form.html.twig' %}
    <div>
        <button type="submit" class="btn btn-primary">{{ 'admin.variant.edit.save'|trans }}</button>
        <a href="{{ path('ir_product_admin_product_show', {'id' : variant.product.id}) }}" class="btn">{{ 'admin.variant.edit.cancel'|trans }}</a>         
    </div>        
</form>